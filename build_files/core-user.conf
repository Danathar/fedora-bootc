# Create 'core' user with a home directory and bash shell, and add to wheel.
# systemd-sysusers processes this at boot, updating /etc/passwd safely. 
u core - "Core User" /home/core /bin/bash
m core wheel

